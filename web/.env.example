# Hosts allowed to load the dev server's chunks, beyond localhost. Needed to view
# `npm run dev` from a phone or another machine on the same network; without it the page
# renders but never becomes interactive, which looks like the site being broken.
#
#   cp .env.example .env.local   and set your machine's LAN address
NEXT_DEV_ORIGINS=
